<%
    function formatToRupiah(amount) {
        return "Rp " + amount.toFixed(2).replace(/\d(?=(\d{3})+\.)/g, "$&,");
    }
%>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <!-- http://localhost:3000/markOrderPurchased?orderId=<%= orderId %> -->
    <section>
        <div class="container card order">
            <% ordersWithItems.forEach(order => { %>
                <div class="container">
                    <div class="centered">
                        <% if (order.purchased) {%>
                            <h2>Order Resolved</h2>
                        <%}%>
                        <% if (!order.purchased) {%>
                            <h2>Order Pending</h2>
                        <%}%>
                    </div>
                    <p><%=userMap[order.userId].username%></p>
                    <ul>
                        <% order.items.forEach(item => { %>
                            <li><%= item.quantity %> <%= item.itemData.name %>: <span style="float: right"><%=formatToRupiah(item.quantity*item.itemData.price)%></span></li>
                        <% }); %>
                    </ul>
                    <div style="text-align: right">
                        <h2>Total: <%=formatToRupiah(order.totalPrice)%></h2>

                        <div class="centered">
                            <p>Show this code to a worker to finish your order</p>
                            <img src="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=http://localhost:3000/api/markOrderPurchased?orderId=<%= orderId %>">
                            <p>Order ID: <%= order._id %></p>   
                        </div>
                        <div class="centered">
                            <a href="/api/cancelOrder" id="cancelOrderBtn" class="btn light-btn">Cancel Order</a>
                            <a href="/" class="btn light-btn">Back to Home</a>
                        </div>
                    </div>
                    
                </div>
            <% }); %>
        </div> 
    </section>

    
    
</body>
</html>

